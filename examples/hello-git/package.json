{
  "name": "hello-git",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "create-and-clone": "bun --env-file .env.local create-and-clone.ts",
    "resume": "bun --env-file .env.local resume.ts"
  },
  "dependencies": {
    "@giselles-ai/sandkit": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
