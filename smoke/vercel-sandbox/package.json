{
  "name": "smoke-vercel-sandbox",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "bun run index.ts",
    "smoke:session": "bun run session-smoke.ts",
    "smoke:codex-exec": "bun run codex-exec-smoke.ts",
    "smoke:drizzle": "bun run drizzle-smoke.ts",
    "smoke:corruption": "bun run corruption-smoke.ts",
    "smoke:policy": "bun run policy-smoke.ts",
    "smoke:policy-credentials": "bun run policy-credentials-smoke.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.2",
    "sandkit": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.3.11"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
